# Migrate to Multi-Sprint - Convert existing BMAD project to multi-sprint structure
name: "migrate-to-multi-sprint"
description: "Migrate an existing BMAD project from single-sprint to multi-sprint agile structure. Handles epic field updates, sprint file creation, velocity log, and preserves correct-course history."

author: "BMad Method"

# Self-describing metadata for workflow discovery
phase: anytime
code: MTMS
agent: sm
sequence: 100

config_source: "{project-root}/_bmad/bmm/config.yaml"
user_name: "{config_source}:user_name"
communication_language: "{config_source}:communication_language"
user_skill_level: "{config_source}:user_skill_level"
document_output_language: "{config_source}:document_output_language"
date: system-generated
implementation_artifacts: "{config_source}:implementation_artifacts"
planning_artifacts: "{config_source}:planning_artifacts"

# Input files to discover
input_file_patterns:
  epics:
    description: "Current epic files to migrate"
    whole: "{planning_artifacts}/*epic*.md"
    sharded: "{planning_artifacts}/*epic*/*.md"
    load_strategy: "FULL_LOAD"
  
  sprint_status:
    description: "Legacy sprint-status.yaml (if exists)"
    whole: "{implementation_artifacts}/sprint-status.yaml"
    load_strategy: "FULL_LOAD"
  
  change_proposals:
    description: "Approved sprint change proposals"
    whole: "{planning_artifacts}/sprint-change-proposal-*.md"
    load_strategy: "FULL_LOAD"
  
  stories:
    description: "Existing story files for reference"
    whole: "{implementation_artifacts}/*.md"
    load_strategy: "INDEX_GUIDED"

# Output locations
installed_path: "{project-root}/_bmad/bmm/workflows/4-implementation/migrate-to-multi-sprint"
instructions: "{installed_path}/instructions.md"
checklist: "{installed_path}/checklist.md"
steps_dir: "{installed_path}/steps"

# Output directories
sprints_dir: "{implementation_artifacts}/sprints"

# Templates
templates:
  sprint: "{installed_path}/templates/sprint-migration-template.yaml"
  velocity_log: "{installed_path}/templates/velocity-log-template.yaml"
  epic_update: "{installed_path}/templates/epic-story-fields.md"

# Steps (sequential execution)
steps:
  analyze: "{steps_dir}/step-migrate-01-analyze.md"
  generate: "{steps_dir}/step-migrate-02-generate.md"
  migrate_epic: "{steps_dir}/step-migrate-02b-migrate-epic.md"
  archive: "{steps_dir}/step-migrate-03-archive.md"
  sprint_history: "{steps_dir}/step-migrate-04-sprint-history.md"
  velocity_log: "{steps_dir}/step-migrate-05-velocity-log.md"
  finalize: "{steps_dir}/step-migrate-06-finalize.md"
